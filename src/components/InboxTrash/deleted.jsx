import React from "react";


const Delete = ({setDelete, filteredEmails }) => {

    const handleDelete = () => {
        
    };

    return (
        <button className="deleteButton" onClick={handleDelete}>
              Delete  
        </button>
    )
}